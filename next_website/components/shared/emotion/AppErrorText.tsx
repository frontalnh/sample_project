import styled from '@emotion/styled';
import { FormHelperText } from '@mui/material';

const AppErrorText = styled(FormHelperText)`
  font-size: 12px;
  font-weight: 500;
  margin: 0;
  padding-left: 4px;
  color: #ff0000;
`;

export default AppErrorText;
